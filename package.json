{
  "name": "@ife/fe-common",
  "version": "0.0.1",
  "description": "fe common modules",
  "author": "liuhui",
  "license": "MIT",
  "scripts": {
    "clean": "rimraf dist",
    "build": "rimraf dist && tsc -p tsconfig.json",
    "start": "ts-node -r tsconfig-paths/register src/index.ts",
    "start:dev": "ts-node -r tsconfig-paths/register src/index.ts",
    "start:prod": "node dist/index.js",
    "test": "mocha -r tsconfig-paths/register -r ts-node/register --recursive -R spec test/*.ts"
  },
  "dependencies": {},
  "devDependencies": {
    "@nestjs/common": "^6.5.3",
    "@nestjs/core": "^6.5.3",
    "@nestjs/platform-express": "^6.5.3",
    "@types/mocha": "^5.2.7",
    "@types/node": "^12.7.1",
    "log4js": "^4.5.1",
    "mocha": "^6.2.0",
    "rimraf": "^2.6.3",
    "rxjs": "^6.5.2",
    "ts-node": "^8.3.0",
    "tsconfig-paths": "^3.8.0",
    "reflect-metadata": "^0.1.13",
    "typescript": "^3.5.3"
  },
  "files": [
    "dist"
  ],
  "publishConfig": {
    "registry": "http://registry.npm.ihomefnt.org"
  }
}
